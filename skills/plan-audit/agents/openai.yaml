interface:
  display_name: "Plan Audit"
  short_description: "Generic plan audit and plan-backed code review"
  default_prompt: "Use $plan-audit to audit the requested plan or plan-backed implementation in the selected mode. Return grounded findings within the authorized scope without turning review into execution."

policy:
  allow_implicit_invocation: false
