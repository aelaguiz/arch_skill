interface:
  display_name: "Arch Docs"
  short_description: "Run a delete-first docs audit that retires stale point-in-time docs"
  default_prompt: "Use $arch-docs for the selected documentation retirement pass. Ground decisions in current code and reader needs, preserve repository deletion and history rules, and run a continuing loop only when explicitly requested."

policy:
  allow_implicit_invocation: false
