interface:
  display_name: "Cynical Code Review"
  short_description: "Skeptical implementation-integrity code review"
  default_prompt: "Use $cynical-code-review to review the requested implementation skeptically against current code and authorized scope. Return grounded findings in the skill's review artifact; do not implement repairs."

policy:
  allow_implicit_invocation: false
