interface:
  display_name: "Audit Loop"
  short_description: "Run an exhaustive map-first repo audit loop with a root audit ledger; supports native goal-mode auto continuation"
  default_prompt: "Use $audit-loop to run the requested repo-wide audit pass or loop, choose consequential in-scope defects, repair them, and review each resulting change. Respect the selected duration and stopping condition."

policy:
  allow_implicit_invocation: false
