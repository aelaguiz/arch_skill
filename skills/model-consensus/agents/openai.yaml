interface:
  display_name: "Model Consensus"
  short_description: "Converge two participants with per-role transport"
  default_prompt: "Use $model-consensus to have the two selected participants independently examine the question, critique each other's evidence, and converge or name the unresolved decision. Preserve the approved destination and exact participant continuity."

policy:
  allow_implicit_invocation: false
