interface:
  display_name: "Audit Loop Sim"
  short_description: "Run an exhaustive map-first real-app automation loop with a root simulator ledger; supports native goal-mode auto continuation"
  default_prompt: "Use $audit-loop-sim to run the requested automation audit pass or loop on the sanctioned device lane. Repair the selected in-scope gap and verify useful behavior; release task-owned device and process resources."

policy:
  allow_implicit_invocation: false
