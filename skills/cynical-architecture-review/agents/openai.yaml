interface:
  display_name: "Cynical Architecture Review"
  short_description: "Find accidental architecture, sprawl, split ownership, and needless complexity."
  default_prompt: "Use $cynical-architecture-review to review the requested architecture for accidental complexity and competing ownership while preserving the authorized user experience and scope. Return findings without implementing changes."

policy:
  allow_implicit_invocation: false
