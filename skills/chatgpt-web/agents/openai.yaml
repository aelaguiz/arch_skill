interface:
  display_name: "ChatGPT Web"
  short_description: "Query logged-in ChatGPT through BrowserOS"
  default_prompt: "Use $chatgpt-web for this consultation. Read its full entry and required companions. Apply prompt-authoring to the actual request and attachments, preserve Pro's expert judgment, verify complete delivery, and return the resulting assessment in the user's preferred form."

dependencies:
  tools:
    - type: "mcp"
      value: "browseros"
      description: "BrowserOS access to the logged-in ChatGPT web session"

policy:
  allow_implicit_invocation: true
