interface:
  display_name: "North Star Investigation"
  short_description: "Run the standalone quantified investigation loop with ranked hypotheses and brutal tests"
  default_prompt: "Use $north-star-investigation to run the selected quantitative investigation with ranked hypotheses and tests that distinguish them. Keep the authorized outcome and stopping condition explicit."

policy:
  allow_implicit_invocation: false
