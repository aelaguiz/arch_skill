interface:
  display_name: "Cynical Cruft Removal"
  short_description: "Find low-value repo artifacts that should go away."
  default_prompt: "Use $cynical-cruft-removal to review the requested scope for low-value artifacts and return grounded deletion candidates. Preserve authorized scope; do not delete files or implement changes."

policy:
  allow_implicit_invocation: false
