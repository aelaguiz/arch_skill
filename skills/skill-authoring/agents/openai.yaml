interface:
  display_name: "Skill Authoring"
  short_description: "Create prompt-first reusable skills"
  default_prompt: "Use $skill-authoring to design or repair this skill. Preserve user intent, put critical instructions first, apply its size and complete-loading guidance, and validate the resulting workflow."

policy:
  allow_implicit_invocation: true
