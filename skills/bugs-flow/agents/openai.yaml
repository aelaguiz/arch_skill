interface:
  display_name: "Bugs Flow"
  short_description: "Run the standalone analyze, fix, and review bug workflow against one bug doc"
  default_prompt: "Use $bugs-flow to run the requested analyze, fix, or review operation against one bug document. Preserve the user's action scope, keep hypotheses challengeable, and add review only when requested."

policy:
  allow_implicit_invocation: false
