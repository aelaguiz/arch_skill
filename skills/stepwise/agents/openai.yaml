interface:
  display_name: "Stepwise"
  short_description: "Run ordered skill steps with exact-worker repair"
  default_prompt: "Use $stepwise to execute the selected ordered process with clean roles and exact-worker repair under the shared agent policy. Preserve the authorized scope and verify task-owned resource cleanup at handoff."

policy:
  allow_implicit_invocation: false
