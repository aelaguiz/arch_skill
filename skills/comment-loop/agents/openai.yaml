interface:
  display_name: "Comment Loop"
  short_description: "Run an exhaustive map-first repo comment hardening loop with a root comment ledger; supports native goal-mode auto continuation"
  default_prompt: "Use $comment-loop to run the requested comment-hardening pass or loop and improve only explanations that materially help readers. Preserve behavior and the selected stopping condition."

policy:
  allow_implicit_invocation: false
