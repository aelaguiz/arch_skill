interface:
  display_name: "Plan Interview"
  short_description: "Interview-driven intent capture producing an approved Intent Pack"
  default_prompt: "Use $plan-interview to run the requested intent interview, grounding questions in source evidence and recording the authorized destination. Stop at the requested intent-capture boundary."

policy:
  allow_implicit_invocation: false
