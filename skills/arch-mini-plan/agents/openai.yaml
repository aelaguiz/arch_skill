interface:
  display_name: "Arch Mini Plan"
  short_description: "Write a standalone one-pass mini architecture plan and hand off follow-through to miniarch-step or arch-step"
  default_prompt: "Use $arch-mini-plan to create or repair the selected one-pass architecture plan. Preserve the authorized destination and stop at the requested planning boundary."

policy:
  allow_implicit_invocation: false
