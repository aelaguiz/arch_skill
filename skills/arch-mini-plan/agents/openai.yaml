interface:
  display_name: "Arch Mini Plan"
  short_description: "Write a standalone one-pass mini architecture plan and hand off follow-through to arch-step"
  default_prompt: "Use $arch-mini-plan to create or repair a one-pass mini architecture plan with canonical arch blocks, then end with the exact arch-step handoff and the later `arch-docs` docs-cleanup handoff after the clean code audit."

policy:
  allow_implicit_invocation: true
