interface:
  display_name: "Fresh Consult"
  short_description: "Run a clean independent read-only reviewer"
  default_prompt: "Use $fresh-consult to obtain the requested independent read-only opinion using clean context and the shared agent policy. Resume the exact reviewer for a follow-up, and return grounded findings with the consultation result."

policy:
  allow_implicit_invocation: false
