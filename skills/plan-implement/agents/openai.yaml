interface:
  display_name: "Plan Implement"
  short_description: "Plan-backed implementation with resumable review state"
  default_prompt: "Use $plan-implement to implement the authorized plan with this workflow's compact progress and review state. Preserve scope, reuse valid checks, and apply the shared agent policy when delegation is authorized."

policy:
  allow_implicit_invocation: false
