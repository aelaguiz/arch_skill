interface:
  display_name: "Miniarch Step"
  short_description: "Run the trimmed full-arch workflow"
  default_prompt: "Use $miniarch-step to perform the requested miniarch-step operation against the canonical plan, preserving the authorized scope and selected stopping condition."

policy:
  allow_implicit_invocation: false
