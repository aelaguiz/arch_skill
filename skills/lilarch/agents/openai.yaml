interface:
  display_name: "Lilarch"
  short_description: "Run the compact 1-3 phase feature flow and escalate to arch-step if it outgrows the fit"
  default_prompt: "Use $lilarch for a contained feature or improvement that should fit in 1-3 phases, and keep the doc and worklog honest all the way through finish mode."

policy:
  allow_implicit_invocation: true
