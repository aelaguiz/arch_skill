interface:
  display_name: "Lilarch"
  short_description: "Run the compact 1-3 phase feature flow and escalate to arch-step if it outgrows the fit"
  default_prompt: "Use $lilarch to run the selected compact start/plan/finish workflow for this feature, preserving the authorized scope and one plan."

policy:
  allow_implicit_invocation: false
