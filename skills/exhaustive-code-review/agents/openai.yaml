interface:
  display_name: "Exhaustive Code Review"
  short_description: "Prompt-only exhaustive code review saved to disk"
  default_prompt: "Use $exhaustive-code-review to review the requested code with the skill's systematic coverage and findings artifact. Stay read-only and use native review slices only within authorized delegation."

policy:
  allow_implicit_invocation: false
