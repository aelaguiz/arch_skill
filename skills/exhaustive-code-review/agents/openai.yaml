interface:
  display_name: "Exhaustive Code Review"
  short_description: "Exhaustive catalog-driven code review saved to disk"
  default_prompt: "Use $exhaustive-code-review to review the requested code against the skill's review catalog and save the findings artifact. Stay read-only. If I name a worker type or count, divide the catalog slices among that many clean native review children and verify every reported finding yourself."

policy:
  allow_implicit_invocation: false
