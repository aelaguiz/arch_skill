interface:
  display_name: "Conductor"
  short_description: "Plan-or-outcome conductor with executive shaping and a pinned worker fleet"
  default_prompt: "Use $conductor to conduct the authorized plan or outcome with delegated implementation and parent review under the shared agent policy. Preserve the approved scope and requested model; use only the selected workflow's required reviews."

policy:
  allow_implicit_invocation: false
