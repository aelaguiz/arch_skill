interface:
  display_name: "Bottom-Up Population Analysis"
  short_description: "Analyze populations, not software bugs"
  default_prompt: "Use $bottom-up-diagnostic to analyze this bounded population from constituent observations, inspect cohorts and outliers, and reaggregate a conclusion whose confidence matches coverage. Report defect signals without starting a repair workflow."

policy:
  allow_implicit_invocation: false
